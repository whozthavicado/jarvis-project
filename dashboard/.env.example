# Copy to .env.local (Next.js's own convention) to point the dashboard at a
# non-default jarvis/server.py instance. Optional — omitting it defaults to
# ws://localhost:8765, which is what `python -m jarvis.server` listens on.
NEXT_PUBLIC_ZERO_WS_URL=ws://localhost:8765
